$schema: 'https://moonrepo.dev/schemas/template.json'

title: 'go'

description: |
    Scaffolds the initial structure for an go,
    including DDD and test folders, a go.mod, and more.

variables:
    name:
        type: 'string'
        default: ''
        required: true
        prompt: 'Package name?'
