pre-commit:
    commands:
        pre-commit:
            interactive: true
            run: mise run pre-commit

prepare-commit-msg:
    commands:
    msg:
        interactive: true
        run: |
            scripts/prepare-commit-msg

# post-merge:
#     commands:
#     pnpm:
#         glob: "{package.json,package-lock.json}"
#         run: |
#             npm install

commit-msg:
    scripts:
        "template_checker":
            runner: bash
